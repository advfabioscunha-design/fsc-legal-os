# Variáveis do frontend (configurar na Vercel → Project → Settings → Environment Variables)

# API do backend — use HTTPS (ver guia: ativar Caddy no servidor)
NEXT_PUBLIC_API_URL=https://api.fscadvocaciadigital.com.br

# Supabase (projeto FSC)
NEXT_PUBLIC_SUPABASE_URL=https://midywtybplbdqmkxfoyj.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=   # Supabase → Settings → API → "anon public"

# WhatsApp do escritório (DDI+DDD+número, só dígitos) — botão do site
NEXT_PUBLIC_WHATSAPP_NUMBER=     # ex.: 5569999999999
